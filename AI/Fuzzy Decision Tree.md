It's just a [[Decision Tree]] where you calculate entropy based on how much each entry belongs to a value of an attribute.

For example if the range of data for temperature is: -20 to 60 degrees Celsius, we can consider that the temperature is either: low, medium or high and we assign let's say 10 degrees Celsius the following (0.2, 0.6, 0.2).